# StockBench 记忆与消息系统升级成果总结

**升级完成时间**: 2025年12月  
**升级版本**: v2.0 → v3.0  
**状态**: ✅ 已完成

---

## 一、升级背景与目标

### 1.1 升级前的问题

| 问题 | 描述 |
|------|------|
| **存储分散** | 缓存在 `storage/cache/`，状态在内存，无统一入口 |
| **history 与缓存混淆** | 缓存是"加速"，history 是"记忆"，代码未区分 |
| **无跨运行持久化** | Pipeline 结束即丢失决策上下文 |
| **检索方式单一** | 只能按时间/key 查询 |
| **无标准消息格式** | LLM 通信使用裸字典，无类型安全 |

### 1.2 升级目标

1. **统一存储后端**：消除分散的存储逻辑
2. **分层记忆架构**：Cache（加速）vs Memory（记忆）分离
3. **结构化决策历史**：history 升级为 EpisodicMemory
4. **标准化消息格式**：Message 类型安全 + 元数据支持
5. **PipelineContext 集成**：统一上下文 + 记忆入口

---

## 二、升级成果概览

### 2.1 新增模块

```
stockbench/
├── memory/                    ← 🆕 记忆系统模块
│   ├── __init__.py
│   ├── schemas.py             # MemoryItem, DecisionEpisode
│   ├── store.py               # MemoryStore 统一入口
│   ├── backends/
│   │   ├── base.py            # StorageBackend 抽象
│   │   └── file_backend.py    # 文件存储实现
│   └── layers/
│       ├── cache.py           # CacheStore
│       ├── working.py         # WorkingMemory
│       └── episodic.py        # EpisodicMemory
│
├── core/
│   ├── message.py             ← 🆕 消息系统
│   └── pipeline_context.py    ← 🔄 集成 memory + conversation_history
│
└── storage/
    └── memory/
        └── episodes/          ← 🆕 情景记忆存储目录
```

### 2.2 实现状态

| 组件 | 状态 | 说明 |
|------|------|------|
| **MemoryStore** | ✅ 完成 | 三层记忆统一入口 |
| **CacheStore** | ✅ 完成 | 兼容现有 `storage/cache/` 结构 |
| **WorkingMemory** | ✅ 完成 | 容量限制 + TTL + 关键词搜索 |
| **EpisodicMemory** | ✅ 完成 | 多维查询 + 关键词搜索 + 结果回填 |
| **Message** | ✅ 完成 | 工厂方法 + 元数据 + API 格式转换 |
| **PipelineContext 集成** | ✅ 完成 | memory 属性 + conversation_history |
| **单元测试** | ✅ 完成 | test_memory_system.py + test_message_system.py |

---

## 三、三层记忆架构

### 3.1 架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      记忆层次 (Memory Hierarchy)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   Layer 3: EpisodicMemory (情景记忆)                                         │
│   │  • 决策历史记录（原 history 升级版）                                       │
│   │  • 结构化存储：时间 + 品种 + 动作 + 理由 + 结果                             │
│   │  • 支持：时间/品种/策略/标签多维检索 + 关键词搜索                            │
│   │  • 结果回填机制（形成闭环学习）                                             │
│   │  • 存储：JSONL 文件（按月份分文件）                                        │
│   │                                                                         │
│   Layer 2: WorkingMemory (工作记忆)                                          │
│   │  • 单次运行的临时上下文                                                   │
│   │  • 容量限制 + TTL 自动清理                                               │
│   │  • 重要性排序 + 关键词搜索                                                │
│   │  • 存储：纯内存                                                          │
│   │                                                                         │
│   Layer 1: CacheStore (缓存层)                                               │
│      • LLM 响应缓存、数据缓存                                                 │
│      • 兼容现有 storage/cache/ 结构                                          │
│      • Key-Value 精确匹配                                                    │
│      • 存储：JSON 文件                                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 各层职责

| 记忆层 | 职责 | 生命周期 | 存储方式 | 检索方式 |
|--------|------|----------|----------|----------|
| **CacheStore** | 加速重复计算 | 可过期 | JSON 文件 | Key-Value |
| **WorkingMemory** | 运行时上下文 | 单次运行 | 纯内存 | 关键词 + 重要性 |
| **EpisodicMemory** | 决策历史 | 滑动窗口 | JSONL 文件 | 时间 + 关键词 + 标签 |

---

## 四、核心组件详解

### 4.1 MemoryStore（统一入口）

**文件**: `stockbench/memory/store.py`

```python
from stockbench.memory import MemoryStore, DecisionEpisode

memory = MemoryStore(base_path="storage")

# 缓存层
memory.cache.get("llm", key)
memory.cache.set("llm", key, value)

# 工作记忆
memory.working.add("当前分析结果...", importance=0.7)
memory.working.search("BTC 突破")

# 情景记忆
memory.episodes.add(decision_episode)
memory.episodes.query(symbol="BTC", days=3)
memory.episodes.search("高波动 止损")
memory.episodes.get_for_prompt("AAPL", n=5)  # 兼容原 history 用法
```

### 4.2 DecisionEpisode（决策记录）

**文件**: `stockbench/memory/schemas.py`

```python
@dataclass
class DecisionEpisode:
    # 基本信息
    id: str
    timestamp: datetime
    date: str                  # YYYY-MM-DD
    
    # 决策内容
    symbol: str
    strategy: str
    action: str                # increase | decrease | hold | close
    target_amount: float
    confidence: float          # 0.0-1.0
    
    # 决策依据
    reasoning: str             # LLM 给出的理由
    reasons: List[str]         # 结构化理由列表
    market_context: Dict       # 市场状态快照
    signals: Dict              # 触发的信号
    
    # 结果回填（形成闭环）
    actual_result: Optional[float]  # 收益率
    outcome_note: Optional[str]
    filled_at: Optional[datetime]
    
    # 索引
    tags: List[str]            # ["高波动", "突破", "止损"]
    importance: float
    related_messages: List[Dict]  # 关联的 Message
```

### 4.3 Message（消息系统）

**文件**: `stockbench/core/message.py`

```python
from stockbench.core.message import Message, build_conversation

# 创建消息（工厂方法）
msg1 = Message.system("你是一个交易分析助手")
msg2 = Message.user("分析 AAPL 的走势")
msg3 = Message.assistant("根据技术指标...")

# 添加元数据
msg = Message.user("分析走势").with_metadata(symbol="AAPL", date="2025-01-01")

# 构建对话
messages = build_conversation(
    system_prompt="You are an analyst",
    history=conversation_history,
    current_user_content="Analyze AAPL"
)

# 转换为 LLM API 格式
api_messages = messages_to_api_format(messages)
```

### 4.4 PipelineContext 集成

**文件**: `stockbench/core/pipeline_context.py`

```python
ctx = PipelineContext(
    run_id="backtest_2025_01_01",
    date="2025-01-01",
    llm_client=llm,
    llm_config=cfg,
    config=config
)

# 记忆系统访问
if ctx.memory_enabled:
    history = ctx.memory.episodes.get_for_prompt(symbol, n=5)
    ctx.memory.working.add("分析结论...", importance=0.8)

# 对话历史
ctx.add_to_history(Message.user(user_content))
ctx.add_to_history(Message.assistant(response))

# 运行结束时
ctx.on_run_complete()  # 自动提交工作记忆到情景记忆
```

---

## 五、History → EpisodicMemory 升级对比

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    升级前 vs 升级后                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   旧 history (简单)                   新 EpisodicMemory (结构化)              │
│   ─────────────────                   ───────────────────────               │
│                                                                             │
│   {                                   {                                     │
│     "date": "2025-12-10",              "id": "ep_20251210_001",             │
│     "symbol": "AAPL",                  "timestamp": "...",                  │
│     "action": "buy",         →         "symbol": "AAPL",                    │
│     "amount": 5000                     "strategy": "breakout_v2",           │
│   }                                    "action": "increase",                │
│                                        "target_amount": 5000,               │
│                                        "reasoning": "突破前高...",           │
│                                        "market_context": {...},             │
│                                        "actual_result": -2.3,  ← 回填       │
│                                        "tags": ["突破", "高波动"]            │
│                                      }                                      │
│                                                                             │
│   增强点:                                                                    │
│   ✓ 结构化 reasoning（不只是动作，还有为什么）                                 │
│   ✓ 市场上下文快照（便于事后分析）                                             │
│   ✓ 标签系统（支持语义检索）                                                   │
│   ✓ 结果回填（形成闭环学习）                                                   │
│   ✓ 关联消息（与 Message 系统打通）                                            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 六、测试覆盖

### 6.1 Memory 系统测试

**文件**: `tests/test_memory_system.py`

| 测试类 | 覆盖内容 |
|--------|----------|
| `TestWorkingMemory` | 添加、搜索、容量限制、重要性淘汰 |
| `TestEpisodicMemory` | 添加、查询、搜索、结果回填 |
| `TestCacheStore` | 缓存读写、删除 |
| `TestMemoryStore` | 初始化、统计、清空 |
| `TestDecisionEpisode` | 序列化、反序列化、可搜索文本 |
| `TestMemoryItem` | 序列化、反序列化 |

### 6.2 Message 系统测试

**文件**: `tests/test_message_system.py`

| 测试类 | 覆盖内容 |
|--------|----------|
| `TestMessage` | 工厂方法、元数据、序列化 |
| `TestApiFormat` | API 格式转换 |
| `TestBuildConversation` | 对话构建 |
| `TestTruncateHistory` | 历史截断 |
| `TestEstimateTokens` | Token 估算 |

---

## 七、配置说明

### 7.1 config.yaml 配置项

```yaml
memory:
  enabled: true                    # 是否启用记忆系统
  storage_path: "storage"          # 存储根目录
  
  working_memory:
    capacity: 50                   # 容量限制
    ttl_minutes: 60                # 过期时间（分钟）
  
  episodic_memory:
    max_days: 30                   # 保留天数
    auto_cleanup: true             # 自动清理过期数据
  
  conversation_history:
    enabled: true                  # 是否启用对话历史
    max_messages: 20               # 最大消息数
```

---

## 八、兼容性保证

| 组件 | 兼容策略 |
|------|----------|
| 现有缓存 `storage/cache/` | 不迁移，CacheStore 直接读写现有文件 |
| `generate_json()` | 保留原接口，可内部重构为调用 Message API |
| Agent 代码 | 渐进式迁移，可逐个升级 |
| 配置文件 | `memory.enabled: false` 可关闭整个系统 |

---

## 九、可选扩展点（未来）

| 扩展 | 描述 | 依赖 | 优先级 |
|------|------|------|--------|
| 向量检索 | 语义搜索 | sentence-transformers | P2 |
| KnowledgeMemory | 知识/规则提炼 | 无 | P2 |
| 反思机制 | 从失败决策学习 | LLM | P3 |
| SQLite 后端 | 更高效的存储 | sqlite3 | P2 |

---

## 十、关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 记忆层数 | 3层 (Cache + Working + Episodic) | 简单优先，满足当前需求 |
| Cache 是否迁移 | 不迁移，只加统一接口 | 兼容现有代码，避免风险 |
| History 升级 | 结构化 + 标签 + 结果回填 | 保持简单，增强可用性 |
| 检索方式 | 关键词匹配 + 时间/标签过滤 | 无外部依赖，实现简单 |
| 存储后端 | JSONL 文件 | 开发简单，易于调试 |
| 向量检索 | 可选扩展，非必须 | 先验证基础功能再加 |

---

## 十一、参考来源

- **《记忆与检索》教程** - HelloAgents 记忆系统设计参考
- **《Agent框架如何构建》** - Agent 架构参考

---

*文档版本: v1.0*  
*创建时间: 2025-12-13*  
*本文档整合自原 5 个升级规划文档，总结已实现的升级成果*
